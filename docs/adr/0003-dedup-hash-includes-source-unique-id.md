# 重複防止ハッシュにソース側の一意識別子を含める

`t_transactions.source_hash`は当初、口座ID・日付・金額・摘要のみから算出する案だったが、同一店舗で同日同額の別取引が発生すると誤って重複扱いされる問題がある。そのため、IMAP取込ではメールヘッダの`Message-ID`、CSV取込では行を一意に特定できる情報を算出元に含めることにした。これにより「同一メール/同一行の再取込」だけを防ぎ、正当な同額取引を誤って捨てるリスクを避ける。ただし、カード会社側が同一内容を複数の別メールとして送るような稀なケースは重複として残る可能性があるが、許容する。
