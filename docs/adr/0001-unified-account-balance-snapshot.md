# 口座残高スナップショットを証券・現金で共通テーブルにする

「総資産額の推移」ウィジェットは投資性資産（証券）と現金性資産（銀行・QR決済残高）の合計を表示する必要がある。証券専用の時価評価テーブルと現金残高専用のテーブルに分割する案もあったが、両方を `t_asset_snapshots` に統合し、`asset_class` で区別する設計を採用した。現金口座では `ticker_or_name` に口座名をそのまま格納する。テーブルを分けると集計クエリが2系統になり、将来の資産クラス追加（暗号資産等）のたびに新テーブルが必要になるため、単一テーブルの方が拡張性が高いと判断した。
